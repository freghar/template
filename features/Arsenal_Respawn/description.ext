class CfgFunctions
{
    class A3MT
    {
        class Arsenal_Respawn
        {
            file = "features\Arsenal_Respawn\functions";
            class loadSpawnGear;
            class saveSpawnGear;
        };
    };
};

/* don't run onPlayerRespawn.sqf on initial load,
 * we don't rely on this, but it's a slight performance improvement */
respawnOnStart = -1;
